/**
 * Dramatis web client.
 *
 * Deliberately near-empty at this stage. Phase 0 establishes only the contract:
 * the schema and its validator. The client arrives in Phase 1.
 */
export const VERSION = "0.1.0";
